package casino;

import org.junit.Assert;
import org.junit.Test;

/**
 * Unit tests for the Dice Game
 */
public class DiceGameUnitTest {

	/**
	 * When we play the dice game we must be sure that there is no fraud. Are
	 * the dice 1 and dice 2 actually being thrown only once when we play the
	 * dice game?
	 */
	@Test
	public void testThatWeOnlyThrowTheDiceOnce() {
		Assert.fail("To be implemented");
	}

	/**
	 * If we have two 6s, are we returning 1 million? Also we should remember to
	 * avoid the notification to the director during the testing!
	 */
	@Test
	public void testThatTheBiggerPrizeIsObtainedWhenWeHaveTwo6s() {
		Assert.fail("To be implemented");
	}

}
